# lefthook.yml — git hooks for novacode

pre-commit:
  commands:
    format:
      run: npx biome format --write .
      stage_fixed: true

    lint:
      run: npx biome check --write .
      stage_fixed: true

    test:
      run: npx vitest run
