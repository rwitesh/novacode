# lefthook.yml — git hooks for novacode

pre-commit:
  commands:
    format:
      run: bun run format
      stage_fixed: true

    lint:
      run: bun run lint
      stage_fixed: true

    test:
      run: bun test
